<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :タイトル %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :画像 %>
    <%= form.file_field :posts_image %>
  </div>

  <div class="field">
    <%= form.label :紹介文 %>
    <%= form.text_area :content %>
  </div>

  <div class="foods">
    <%= form.fields_for :foods do |f| %>
      <%= f.label "タグ" %>
      <%= render "food_fields", f: f %>
    <% end %>
    <div class="links">
      <%= link_to_add_association "タグを追加", form, :foods %>
    </div>
  </div>

  <div class="recipes">
  <%= form.fields_for :recipes do |f| %>
    <%= f.label "タグ" %>
    <%= render "recipe_fields", f: f %>
  <% end %>
  <div class="links">
    <%= link_to_add_association "タグを追加", form, :recipes %>
  </div>
</div>



  <div class="field">
    <%= form.label :コツ・ポイント %>
    <%= form.text_area :point %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
